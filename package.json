{
  "name": "secure-inquiry-middleware",
  "version": "1.0.0",
  "description": "Secure Express middleware that redacts PII, calls a mock AI service behind a circuit breaker, and writes encrypted audit logs.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest --runInBand"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": ["express", "security", "circuit-breaker", "pii"],
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000
  }
}
